Run the `handoff` operator defined in this repo's `AGENTS.md` (Operator
Requests): update `.agents/state.md` so the next session can resume without chat
context. `AGENTS.md` is the authoritative definition; this file is only a
pointer.
