---
name: catchup
description: Re-ground in this repo's current state and report before making any change. Use when the owner says catchup or asks where things stand.
# toolkit-owned; edits are drift — see AGENTS.md
---

Run the `catchup` operator defined in this repo's `AGENTS.md` (Operator
Requests): re-read `AGENTS.md` (the Prime Invariants in full),
`.agents/state.md`, and the active repo docs, then summarize the current
state, the next action, blockers, and one proposed first action. Make no
changes until the human responds. `AGENTS.md` is the authoritative
definition; this skill is only a pointer.
