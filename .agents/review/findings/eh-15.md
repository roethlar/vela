# eh-15: Badge assertions can pass before the post-refetch card state is proven

**Severity**: MEDIUM — a regression where the refetch restores the old watch state (or drops the card) stays green: the wait is satisfied by the optimistic local mutation or by the empty grid during the refresh gap
**Status**: Verified
**Branch**: n/a — no-branches adaptation; fix lands as a single commit on `main`
**Commit**: `6db391c`

## Evidence
`tests/e2e/scenarios/markwatched.mjs` (pre-fix) — the unwatch wait is
`!card?.classList.contains('watched')`, truthy when the card is MISSING
and immediately after `setWatched`'s optimistic `item.played = played`
(`src/routes/+page.svelte:737`) — before `refreshWatchState()` →
`resetAndLoad()` clears and reloads the grid. `driver.waitFor` returns on
the first truthy poll. The watched-direction wait has the mirrored hole
(optimistic badge before the refetch could clear it). Filed by codex in
the e2e-10 batch pass (unwatch leg); the coder extended it to the
mirrored watched leg.

## Predicted observable failure
A refetch that restores the previous watch state, or a refreshed listing
that drops the card, still exits 0 — the scenario claims "survives the
refetch" without ever observing post-refetch state.

## What
The waits don't distinguish optimistic UI state from refetched server
state.

## Approach
Track the mock's `/Users/{u}/Items` GET count: record it before each
mark action, wait for the action's PlayedItems request, then wait for a
LATER Items refetch, and only then assert the card is PRESENT and carries
(or lacks) the badge.

## Files changed
- `tests/e2e/scenarios/markwatched.mjs` — refetch-count gating + present-
  card badge assertions, both directions

## Guard proof
Executed 2026-07-05, `npm run e2e -- --skip-build markwatched` (debug binary
built the same day, current with HEAD's frontend — no src file newer). The
guard-proof regressions were injected via env-gated stale-refetch modes added
to `tests/e2e/mockjf.mjs` for the proof only and reverted (`git checkout`)
before commit — the committed change touches `markwatched.mjs` alone.

| # | Scenario | Mock mode | Result | Proves |
|---|----------|-----------|--------|--------|
| 1 | fix | true | PASS | happy path intact, both legs |
| 2 | fix | `stale-false` (listing always reports Played:false) | FAIL @ `:113` "watched badge on the refetched card" | the watched-leg assertion catches a refetch that drops the just-set watched state |
| 3 | fix | `sticky-true` (listing reports Played:true once ever-played) | FAIL @ `:144` "watched badge cleared on the refetched card" | the unwatch-leg assertion catches a refetch that resurrects the cleared state |
| 6 | fix | `drop-after-unwatch` (listing drops the card after an unwatch) | FAIL @ `:144` | the unwatch-leg present-card assertion catches a refetch that drops the card — the empty-grid vacuity's target |
| 7 | **OLD (pre-fix)** | `drop-after-unwatch` | **PASS** | **anti-churn:** the old `!card?...watched` wait exits 0 on the missing card — the exact eh-15 hole the fix closes |
| 4 | OLD | `stale-false` | FAIL | the old wait incidentally catches a permanently-wrong *steady* value: the node-side `pollUntil(POST/DELETE)` delays its `waitFor` past the optimistic/empty windows to steady state, so this mock does not isolate the hole |
| 5 | OLD | `sticky-true` | FAIL | same as run 4, unwatch direction |

Run 7 (OLD, PASS) vs run 6 (fix, FAIL) on the identical dropped-card regression
is the load-bearing pair: reverting the fix makes the scenario green on a
regression the fix catches — the strengthening is not churn.

Runs 2/3/6 are deterministic: once the mock records the action's listing
refetch (`itemsRefetches() > before`), `resetAndLoad`'s `items = []` has already
flushed (a microtask, ordered before the cross-process IPC+HTTP that reaches the
mock), so the assertion can only observe the post-refetch card.

## Coder dispute (if any)
None — admitted as filed; scope extended to the mirrored leg.

## Known gaps
Refined during the fix (corroborated by an independent 3-lens adversarial review,
all lenses `refuted:false`): the optimistic **watched** card never paints —
`setWatched`'s `item.played = true` and `resetAndLoad`'s `items = []` run in the
same synchronous tick after the awaited POST, so Svelte 5 coalesces them into one
flush (old card → empty grid), never a standalone optimistic watched frame. The
genuinely vacuous case the fix closes is therefore the **unwatch** leg's
missing-card wait (runs 6/7). The watched-leg strengthening is still load-bearing,
but via the refetch **gate**, not the present-card check: the gate catches a
"refetch never fired" regression that would otherwise pass on the retained
optimistic card. The `stale-false`/`sticky-true` mocks (runs 2/3) exercise the
assertions but, because the old waits also catch a permanently-wrong steady value
(runs 4/5), only `drop-after-unwatch` (runs 6/7) isolates the eh-15 hole itself.

## Reviewer comments
Reviewer: `codex` (codex-cli 0.142.5), read-only, `--output-schema`-enforced
verdict. Reviewed head `6db391c`, base `2bda0e3`. 2026-07-05T17:17Z.
**Verdict: accepted. guard_confirmed: true** (analytical guard-confirm, not
executed — the full harness needs Xvfb + tauri-driver + a built debug binary
headless; consistent with prior e2e loops' manual-check mode).

Comments:
- `markwatched.mjs:110` — the fix gates both badge assertions on a later exact
  `/Users/{u}/Items` listing request then requires a present card, closing the
  old missing-card vacuity.
- `markwatched.mjs:91` — the pre-existing POST/DELETE, mock-state, watchedBefore,
  and contractViolations assertions were retained.
- `eh-15.md:55` — the run 7 (old, PASS) vs run 6 (fixed, FAIL) pair is sound: a
  `drop-after-unwatch` mock satisfies the old optional-chain negation on an absent
  card, while the fixed assertion cannot pass without the refetched card.
